/**
 * Maps a number from one scale to another.
 * @example map(3, 0, 4, -1, 1) -> 0.5
 */
declare function map(val: number, min1: number, max1: number, min2: number, max2: number): number;
export default map;
